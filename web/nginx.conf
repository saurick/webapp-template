server {
    listen 80;
    server_name _;

    # 前端静态文件
    root /usr/share/nginx/html;
    index index.html;

    # 处理前端路由（React Router BrowserRouter）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 把 /rpc 开头的请求转发到后端 server 容器
    location /rpc/ {
        proxy_pass http://baccarat-server:8000;  # 👈 这里的名字等下在 compose 里定义
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}