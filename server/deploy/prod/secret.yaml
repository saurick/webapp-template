apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  namespace: your-project-prod
type: kubernetes.io/dockerconfigjson
data:
  # 用法示例：
  # echo -n '{"auths":{"https://registry.example.com/":{"username":"deploy","password":"replace-me","auth":"base64-user-pass"}}}' | base64
  .dockerconfigjson: e30=
